// SPDX-License-Identifier: MIT
pragma solidity 0.8.18;

contract Verifier {
    function verify(
        uint256[] memory pubInputs,
        bytes memory proof
    ) public view returns (bool) {
      return true;
  }
}
